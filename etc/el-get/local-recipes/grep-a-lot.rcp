(:name grep-a-lot
       :description "manages multiple search results buffers for grep.el."
       :website     "https://github.com/jdufresne/emacs-grep-a-lot"
       :type        github
       :pkgname     "jdufresne/emacs-grep-a-lot"
       :features    grep-a-lot
       :post-init   (grep-a-lot-setup-keys))
